export const metadata = {
    title: "Work & Projects",
    description: "Showcase of my latest UI/UX design projects, including web apps, mobile apps, and branding.",
};

export default function WorkLayout({ children }) {
    return <>{children}</>;
}
