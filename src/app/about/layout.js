export const metadata = {
    title: "About Me",
    description: "Learn more about my background, experience, skills, and design philosophy.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
