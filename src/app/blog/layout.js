export const metadata = {
    title: "Thoughts & Insights",
    description: "Writing about design systems, accessibility, motion, and the future of digital product design.",
};

export default function BlogLayout({ children }) {
    return <>{children}</>;
}
